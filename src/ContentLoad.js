import React from 'react'
import Repo from './Repo'
import Commits from './Comit'

// const {repos} = this.state
const ContentLoad = (repos) => (
  <div className="teste">
    <p>{repos}</p>
  </div>
)

export default ContentLoad
